---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: meshidentityslices.mesh-cni.dev
spec:
  group: mesh-cni.dev
  names:
    categories: []
    kind: MeshIdentitySlice
    plural: meshidentityslices
    shortNames: []
    singular: meshidentityslice
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MeshIdentitySliceSpec via `CustomResource`
        properties:
          spec:
            properties:
              cluster:
                description: Source cluster that owns this mirrored identity slice
                type: string
              endpoints:
                description: Backend pod endpoints for this identity slice
                items:
                  properties:
                    ip:
                      description: Backend pod IP
                      format: ip
                      type: string
                    namedPorts:
                      description: Named ports declared by this pod endpoint
                      items:
                        properties:
                          name:
                            description: Named port identifier
                            type: string
                          port:
                            description: Container port value
                            format: uint16
                            maximum: 65535.0
                            minimum: 0.0
                            type: integer
                          protocol:
                            description: Protocol ("TCP", "UDP", or "SCTP")
                            type: string
                        required:
                        - name
                        - port
                        - protocol
                        type: object
                      type: array
                  required:
                  - ip
                  type: object
                type: array
              namespaceLabels:
                additionalProperties:
                  type: string
                description: Snapshot of namespace labels for namespaceSelector evaluation
                type: object
              podLabels:
                additionalProperties:
                  type: string
                description: Canonical pod labels matched by this identity slice
                type: object
            required:
            - cluster
            type: object
        required:
        - spec
        title: MeshIdentitySlice
        type: object
    served: true
    storage: true
    subresources: {}
