---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusters.mesh-cni.dev
spec:
  group: mesh-cni.dev
  names:
    categories: []
    kind: Cluster
    plural: clusters
    shortNames: []
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ClusterSpec via `CustomResource`
        properties:
          spec:
            properties:
              secret:
                description: Name of the ConfigMap storing the kubeconfig for the cluster
                properties:
                  key:
                    description: Key in secret to get kubeconfig
                    nullable: true
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                required:
                - name
                type: object
            required:
            - secret
            type: object
        required:
        - spec
        title: Cluster
        type: object
    served: true
    storage: true
    subresources: {}
