---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: meshendpoints.mesh-cni.dev
spec:
  group: mesh-cni.dev
  names:
    categories: []
    kind: MeshEndpoint
    plural: meshendpoints
    shortNames: []
    singular: meshendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MeshEndpointSpec via `CustomResource`
        properties:
          spec:
            properties:
              backend_port_mappings:
                description: The backend IP, service port, backend port, and protocol
                items:
                  properties:
                    backend_port:
                      description: The target port defined in the Service
                      format: uint16
                      maximum: 65535.0
                      minimum: 0.0
                      type: integer
                    ip:
                      description: Backend IP
                      format: ip
                      type: string
                    protocol:
                      description: The protocol of the defined port
                      type: string
                    service_port:
                      description: Port defined in Service
                      format: uint16
                      maximum: 65535.0
                      minimum: 0.0
                      type: integer
                  required:
                  - backend_port
                  - ip
                  - protocol
                  - service_port
                  type: object
                type: array
              service_ips:
                description: The ClusterIPs defined in the Service
                items:
                  format: ip
                  type: string
                type: array
            required:
            - backend_port_mappings
            - service_ips
            type: object
        required:
        - spec
        title: MeshEndpoint
        type: object
    served: true
    storage: true
    subresources: {}
