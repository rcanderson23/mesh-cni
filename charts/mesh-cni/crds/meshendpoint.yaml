---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: meshendpoints.mesh-cni.dev
spec:
  group: mesh-cni.dev
  names:
    categories: []
    kind: MeshEndpoint
    plural: meshendpoints
    shortNames: []
    singular: meshendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MeshEndpointSpec via `CustomResource`
        properties:
          spec:
            properties:
              backend_port_mappings:
                items:
                  properties:
                    backend_port:
                      format: uint16
                      minimum: 0.0
                      type: integer
                    ip:
                      format: ip
                      type: string
                    protocol:
                      type: string
                    service_port:
                      format: uint16
                      minimum: 0.0
                      type: integer
                  required:
                  - backend_port
                  - ip
                  - protocol
                  - service_port
                  type: object
                type: array
              service_ips:
                items:
                  format: ip
                  type: string
                type: array
            required:
            - backend_port_mappings
            - service_ips
            type: object
        required:
        - spec
        title: MeshEndpoint
        type: object
    served: true
    storage: true
    subresources: {}
